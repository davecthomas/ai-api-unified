---
agentmemory_version: "0.4.4"
timestamp: "2026-07-08T12:21:10Z"
author: "2355287-davecthomas"
branch: "finops-cached-tokens-pii-fixes"
thread_id: "5772aded-b79f-4a97-9afb-27036fa135be"
turn_id: "0601d61c0f"
workstream_id: "thread-5772aded-b79f-4a97-9afb-27036fa135be"
workstream_scope: "thread"
episode_id: "episode-thread-5772aded-b79f-4a97-9afb-27036fa135be"
episode_scope: "thread"
checkpoint_goal: "Extend the finops observability middleware so emitted per-call USD cost reflects prompt-cache discounts, not just full-rate input/output tokens."
checkpoint_surface: "The logger-backed observability after_call cost path and the per-provider result-summary builders that normalize provider-reported token counts (here the Bedrock completions hook)."
checkpoint_outcome: "Wired cache-read token capture end to end: Bedrock's cacheReadInputTokens is folded into provider_prompt_tokens and surfaced as cached_input_tokens on the cost event, with a covering test."
decision_candidate: false
enriched: true
ai_generated: true
ai_model: "opus[1m]"
ai_tool: "claude"
ai_surface: "claude-code"
ai_executor: "local-agent"
related_adrs:
  - "ADR-0003"
files_touched:
  - "docs/finops_middleware_design.md"
  - "src/ai_api_unified/completions/ai_bedrock_completions.py"
  - "src/ai_api_unified/middleware/observability.py"
  - "tests/test_cached_token_capture.py"
design_docs_touched:
  - "docs/finops_middleware_design.md"
verification:
  - "git diff:  4 files changed, 36 insertions(+), 8 deletions(-); `cached_input_tokens`. Scope is cache *reads*: cache *writes* (priming a; cache, billed above the base input rate \u2014 Bedrock exposes them as; `cacheWriteInputTokens`) are not captured or billed yet, because the pricing"
  - "design doc touched: docs/finops_middleware_design.md"
source_pending_shards:
  - ".agents/memory/pending/2026-07-08/2026-07-08T12-21-10Z--2355287-davecthomas--thread_5772aded-b79f-4a97-9afb-27036fa135be--turn_0601d61c0f.md"
---

## Why

- This advances Phase 3 (2.12.0) of the finops design, which makes cost auditable by billing the cached input subset at the cached rate and the remainder at the full input rate. Without cache-read capture, cached calls are overbilled in the emitted cost events, so this closes an accuracy gap in the cost-attribution stream that billing and budgeting layers downstream depend on.

## What changed

- The result summary now carries a cached-input-token count, and the Bedrock hook extracts its cache-read count (cacheReadInputTokens) and folds it into the prompt-token total so the cached subset is uniform across providers. The observability cost enrichment surfaces cached_input_tokens on the cost event. Scope is deliberately cache reads only.

## Evidence

- docs/finops_middleware_design.md Phase 3 / Decision 3 documents the per-provider cache-read extraction and cached-vs-full-rate split; tests/test_cached_token_capture.py covers the behavior; changes land in src/ai_api_unified/completions/ai_bedrock_completions.py and src/ai_api_unified/middleware/observability.py. Design aligns with ADR-0003 (observability is a lifecycle role that only emits metadata).

## Next

- Cache *writes* (Bedrock cacheWriteInputTokens, billed above the base input rate) are not yet captured or billed because the pricing registry has no cache-write rate column; that is the follow-on once such a column lands.
