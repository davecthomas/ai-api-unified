---
agentmemory_version: "0.4.4"
timestamp: "2026-07-03T00:56:24Z"
author: "2355287-davecthomas"
branch: "dedup-embeddings-media-params"
thread_id: "d3388f5f-3b6f-4cc0-b951-3d4fe643fa66"
turn_id: "9873be4325"
workstream_id: "thread-d3388f5f-3b6f-4cc0-b951-3d4fe643fa66"
workstream_scope: "thread"
episode_id: "episode-main-b34c818a56"
episode_scope: "mixed"
checkpoint_goal: "Consolidate multimodal embeddings media-attachment parameter handling into one shared, validated base model and make the capability shipped in PR #18 release-ready."
checkpoint_surface: "The embeddings parameter-model layer in ai_base.py, the Gemini embeddings provider, and the package's public API/export surface."
checkpoint_outcome: "Documented the media-attachment constraints (20MB caps; API-key auth required, with up-front rejection in service-account mode) in the README, exported the new shared model and capability error publicly, and bumped the version from 2.5.4 to 2.6.0 for the new public surface."
decision_candidate: false
enriched: true
ai_generated: true
ai_model: "claude-fable-5[1m]"
ai_tool: "claude"
ai_surface: "claude-code"
ai_executor: "local-agent"
related_adrs:
files_touched:
  - "README.md"
  - "pyproject.toml"
  - "src/ai_api_unified/__init__.py"
  - "src/ai_api_unified/__version__.py"
  - "src/ai_api_unified/ai_base.py"
  - "src/ai_api_unified/embeddings/ai_google_gemini_embeddings.py"
design_docs_touched:
verification:
  - "git diff:  6 files changed, 202 insertions(+), 262 deletions(-); Media attachments are capped at 20MB per attachment and per request. They also; require API-key auth (`GOOGLE_AUTH_METHOD=api_key`): the google-genai SDK sends; only text parts to the Vertex embedding endpoint, so in service-account mode"
  - "git diff:  6 files changed, 196 insertions(+), 262 deletions(-); AIEmbeddingsCapabilitiesBase,; AIEmbeddingsMultimodalParams,; AIIncludedMediaParamsBase,"
source_pending_shards:
  - ".agents/memory/pending/2026-07-03/2026-07-03T00-57-30Z--2355287-davecthomas--thread_aa399621-1e14-4f1a-9d0d-899553970e30--turn_54c04f2236.md"
  - ".agents/memory/pending/2026-07-03/2026-07-03T00-56-24Z--2355287-davecthomas--thread_d3388f5f-3b6f-4cc0-b951-3d4fe643fa66--turn_9873be4325.md"
---

## Why

- After PR #18 (e19c342) added capability-gated multimodal embeddings via gemini-embedding-2, the media-attachment fields and their aligned-list validation were duplicated across parameter models. AIIncludedMediaParamsBase in ai_base.py now carries the shared fields plus validation, with subclasses declaring accepted media via DICT_ALLOWED_MIME_PREFIXES and byte caps via MAX_MEDIA_BYTES, so validation behavior stays consistent as more providers gain multimodal support. Release-readying this surface also records a user-facing constraint: media embeddings require API-key auth because the google-genai SDK sends only text parts to the Vertex embedding endpoint in service-account mode, so the client rejects media attachments up front rather than silently degrading.

## What changed

- The dedup-embeddings-media-params branch rebased the Gemini embeddings provider onto the new shared AIIncludedMediaParamsBase, removing roughly 260 lines of duplicated model code. This turn finished the release surface: README now documents the 20MB per-attachment and per-request media caps, the GOOGLE_AUTH_METHOD=api_key requirement with fail-fast rejection in service-account mode, and the AiProviderCapabilityUnsupportedError factory failure mode; AIIncludedMediaParamsBase, AIEmbeddingsCapabilitiesBase, AIEmbeddingsMultimodalParams, AiProviderCapabilityUnsupportedError, and SupportedDataType are exported from the package root; version bumped 2.5.4 -> 2.6.0 in pyproject.toml and __version__.py.

## Evidence

- The Gemini embeddings provider enforces the API-key requirement with an explicit error directing users to set GOOGLE_AUTH_METHOD=api_key, confirming the fail-fast behavior the README now documents. AIIncludedMediaParamsBase in ai_base.py implements both the per-attachment and the combined per-request MAX_MEDIA_BYTES checks that back the documented 20MB caps. Merged PR #18 (commit e19c342) is the upstream multimodal-embeddings feature this branch consolidates, the 2026-07-03 daily summary independently corroborates the shared-model refactor, and the API-key constraint ties to ADR-0005 (Google API-key auth default).

## Next

- Run the embeddings capability test suite against the refactored models before merging; neither July-3 capture recorded a test validation signal. Consider migrating OpenAI and Titan embeddings providers onto AIIncludedMediaParamsBase when they gain multimodal inputs, and evaluate whether the service-account fail-fast media rejection deserves ADR treatment alongside ADR-0005.
