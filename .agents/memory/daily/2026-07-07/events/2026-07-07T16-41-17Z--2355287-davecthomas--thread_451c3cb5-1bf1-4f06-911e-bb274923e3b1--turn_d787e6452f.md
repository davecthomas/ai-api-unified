---
agentmemory_version: "0.4.4"
timestamp: "2026-07-07T16:41:17Z"
author: "2355287-davecthomas"
branch: "main"
thread_id: "451c3cb5-1bf1-4f06-911e-bb274923e3b1"
turn_id: "d787e6452f"
workstream_id: "thread-451c3cb5-1bf1-4f06-911e-bb274923e3b1"
workstream_scope: "thread"
episode_id: "episode-main-b34c818a56"
episode_scope: "mixed"
checkpoint_goal: "Advance the FinOps observability backlog: attach real USD cost to every provider call and emit auditable per-call cost events to feed billing, budgeting, and per-caller attribution."
checkpoint_surface: "The YAML-driven observability middleware lifecycle \u2014 generalizing the single get_observability_middleware() slot into a CompositeObservabilityMiddleware chain and adding a cost-tracking middleware beside the existing logger-backed one."
checkpoint_outcome: "Authored and reviewed docs/finops_middleware_design.md, locking v1 scope to observe-only with a pluggable AiApiCostSink and a three-phase delivery plan."
decision_candidate: true
enriched: true
ai_generated: true
ai_model: "opus[1m]"
ai_tool: "claude"
ai_surface: "claude-code"
ai_executor: "local-agent"
related_adrs:
files_touched:
  - "docs/finops_middleware_design.md"
design_docs_touched:
  - "docs/finops_middleware_design.md"
verification:
  - "git diff: 1 new untracked file: docs/finops_middleware_design.md"
  - "design doc touched: docs/finops_middleware_design.md"
source_pending_shards:
  - ".agents/memory/pending/2026-07-07/2026-07-07T16-41-17Z--2355287-davecthomas--thread_451c3cb5-1bf1-4f06-911e-bb274923e3b1--turn_d787e6452f.md"
---

## Why

- This design turns the just-landed structured pricing work (ADR-0009, 2.9.0) into an actual cost-observability capability, so a future agent picking up the finops backlog has a settled contract to implement against: what the cost event carries, where it hooks in, and what is intentionally deferred. It records the load-bearing decision that cost tracking is observe-only in v1 (never affecting program flow), which constrains every later budgeting/enforcement layer built on the event stream.

## What changed

- Established that cost tracking is a lifecycle/observability middleware (ADR-0003), enabled via the YAML middleware profile (ADR-0002), computed in after_call from provider-reported token counts via AIModelPricing.compute_token_cost. The one architectural change is turning the single observability slot into a fail-open composite chain so logging and cost tracking coexist. Defines the auditable AiApiCostEvent (carrying pricing provenance: effective date, source, confidence) and a pluggable AiApiCostSink with a default log sink.

## Evidence

- docs/finops_middleware_design.md; grounds on ADR-0002 (YAML-driven middleware), ADR-0003 (two middleware roles), and ADR-0009/2.9.0 structured pricing; cites the concrete AiApiObservabilityMiddleware.after_call(call_context, call_result_summary) hook and compute_token_cost surface it builds on.

## Next

- Phase 1 is a behavior-preserving CompositeObservabilityMiddleware refactor; cached-token cost accuracy is deferred to Phase 3 because the result summary does not yet capture per-provider cached-token counts (OpenAI prompt_tokens_details.cached_tokens, Gemini cached_content_token_count, Anthropic cache read/write). This is a design-for-review with no code written yet — a candidate for promotion to an ADR.
