# 2026-07-07 summary

## Snapshot

- Captured 11 memory events.
- Main work: Added a research deliverable that normalizes current provider pricing into per-modality canonical units (text/embeddings per 1M tokens split into input/cached/output, speech per 1M chars, images per image, video per second, transcription per minute) and proposes an AIModelPricing model using Decimal money types, effective_date/source provenance, per-modality unit fields, and optional context/quality tiers. It recommends storing rates in a versioned registry keyed by (provider, model) outside the model classes, exposing pricing through the existing for_model() capabilities path, and computing cost from provider-reported input/output token counts while keeping the old float/calculate_cost as deprecated shims for one release.
- Top decision: The current cost model exposes one blended `price_per_1k_tokens` float per model and multiplies it by a raw token count. The research shows this is structurally wrong, not merely imprecise: output is priced 4–8× input (gpt-5.5 is $5 in / $30 out), cached input is now a first-class rate roughly 10× cheaper than fresh input, non-text modalities do not price per token at all (images per image, video per second, TTS per character), and some models tier by context length (gemini-2.5-pro doubles above 200K input tokens). A single scalar cannot express any of these, so blended figures are already fiction — the OpenAI "0.0100" entries are hand-picked midpoints between input and output list prices. ([2026-07-07 11:51:24 UTC by dave-thomas](events/2026-07-07T11-51-24Z--dave-thomas--adr-inspector.md))
- Blockers: The shipped cost model exposes one blended per-token rate and calculate_cost(num_tokens), which is structurally wrong: input vs output are priced asymmetrically (output 4-8x input), cached input is now a distinct ~10x-cheaper rate, non-text modalities bill per image/second/character rather than per token, some models have context-length tiers, and no rate carries an effective date or source. A finops cost ledger cannot be built on that. This research doc is the design foundation that ADR-0009 formalizes, and it gates the finops observability middleware that comes after.

| Metric | Value |
|---|---|
| Memory events captured | 11 |
| Repo files changed | 11 |
| Decision candidates | 9 |
| Active blockers | 2 |

## Major work completed

- Added a research deliverable that normalizes current provider pricing into per-modality canonical units (text/embeddings per 1M tokens split into input/cached/output, speech per 1M chars, images per image, video per second, transcription per minute) and proposes an AIModelPricing model using Decimal money types, effective_date/source provenance, per-modality unit fields, and optional context/quality tiers. It recommends storing rates in a versioned registry keyed by (provider, model) outside the model classes, exposing pricing through the existing for_model() capabilities path, and computing cost from provider-reported input/output token counts while keeping the old float/calculate_cost as deprecated shims for one release.
- `pricing_research.md` documents the replacement of the single blended rate with the `AIModelPricing` / `AIPricingTier` / `AITokenRates` descriptor: a per-modality `unit`, split token rates (input, cached_input, output), per-image / per-second / per-minute fields, an optional `tiers` list for context-length and quality/size tiers, and `Decimal`-typed money. Normalized pricing tables for text, embeddings, images, video, and voice establish the canonical unit per modality.
- `pricing_research.md` establishes pricing as data owned by a versioned registry keyed by `(provider, model)`, separate from provider classes, with per-entry `effective_date` / `source` / `confidence` for auditability, exposed via the capabilities `for_model()` resolution path. The doc also inventories model health (retired gemini-1.5 models, deprecated gemini-2.0 family, imagen-4.0 shutdown) that the registry's provenance is meant to prevent recurring.
- `pricing_research.md` specifies replacing `calculate_cost(num_tokens)` with a cost function driven by real provider-reported input/output token counts and the split per-modality rates, consumed by the planned finops observability middleware on `after_call`, and keeping the old blended API as deprecated one-release shims.
- Added a review-grade design doc that normalizes current provider pricing into per-modality tables (text, embeddings, images, video, voice) with confidence levels and sources, then proposes replacing the blended float with Decimal-typed AIModelPricing/AITokenRates/AIPricingTier models stored in a versioned registry outside the provider classes, resolved through the existing capabilities for_model() path, with the legacy price_per_1k_tokens/calculate_cost kept as deprecated shims for one release. Also includes a model-health audit flagging retired/deprecated Gemini and Imagen models to remove.
- Established that cost tracking is a lifecycle/observability middleware (ADR-0003), enabled via the YAML middleware profile (ADR-0002), computed in after_call from provider-reported token counts via AIModelPricing.compute_token_cost. The one architectural change is turning the single observability slot into a fail-open composite chain so logging and cost tracking coexist. Defines the auditable AiApiCostEvent (carrying pricing provenance: effective date, source, confidence) and a pluggable AiApiCostSink with a default log sink.
- `docs/finops_middleware_design.md` specifies replacing the single-middleware observability slot with a `CompositeObservabilityMiddleware` built by `get_observability_middleware()` from the YAML profile (ADR-0002). The existing logger-backed middleware becomes one child; the new cost middleware becomes another. Fan-out to children preserves the existing fail-open semantics of `execute_observed_call`.
- `docs/finops_middleware_design.md` confirms the scope decision: finops v1 is observe-only and emits per-call cost events, with budgets and enforcement deferred to a later layer that consumes the event stream rather than living in the library.
- `docs/finops_middleware_design.md` specifies the `AiApiCostEvent` shape (including pricing provenance fields), a pluggable `AiApiCostSink` abstraction with a default log sink, and the decision that the library performs no aggregation — emitting per-call events correlated by ids already on the context.
- `docs/finops_middleware_design.md` now specifies that finops v1 adds **no new middleware class and no composite chain**. Cost is computed as a config-gated enrichment within the existing logger-backed observability `after_call` when `emit_cost` is true: resolve `AIModelPricing` for `(provider, model)`, read the provider-reported prompt/completion tokens, compute `usd_cost` via `compute_token_cost`, and emit it on the observability event. This supersedes ADR-0012's decision to generalize the single observability slot into a `CompositeObservabilityMiddleware`; that composite is explicitly deferred until a separation earns its keep.

## Why this mattered

- The library currently prices every model with one blended price_per_1k_tokens float times a token count, which cannot express asymmetric input/output rates, cached-input rates, non-token modalities (image/video/character), context-length tiers, or rate provenance. A finops cost layer needs auditable, real-usage cost math, so the pricing model must be redesigned before the observability middleware can be built. This doc is the design basis for that redesign.
- The current cost model exposes one blended `price_per_1k_tokens` float per model and multiplies it by a raw token count. The research shows this is structurally wrong, not merely imprecise: output is priced 4–8× input (gpt-5.5 is $5 in / $30 out), cached input is now a first-class rate roughly 10× cheaper than fresh input, non-text modalities do not price per token at all (images per image, video per second, TTS per character), and some models tier by context length (gemini-2.5-pro doubles above 200K input tokens). A single scalar cannot express any of these, so blended figures are already fiction — the OpenAI "0.0100" entries are hand-picked midpoints between input and output list prices.
- Today rate data is embedded inside the provider/model classes. That conflates two things that change on completely different schedules and for different reasons: a model class describes fixed behavior and capabilities, whereas a rate is external data the provider revises on its own timeline. The research calls out the consequence — the in-code numbers are stale and partly invented, several advertised models are retired or past their shutdown date, and no rate carries an effective date or source, so no cost figure is auditable. A finops layer cannot stand on unauditable, drift-prone numbers.
- Cost today is computed by `calculate_cost(num_tokens)` — one blended rate times a single token count. Once input, cached-input, and output carry asymmetric rates, blending is wrong regardless of how good the rate table is, because it discards the actual input/output mix of each call. The decision is to change the cost-computation contract: cost is derived from the real, provider-reported usage — the `provider_prompt_tokens` and `provider_completion_tokens` the observability runtime already captures — multiplied by the split rates from the pricing descriptor, yielding an auditable cost event rather than an estimate. This is why the pricing redesign must land before the finops middleware: the middleware hooks `after_call`, reads actual token counts, looks up `AIModelPricing`, and emits `{model, input_tokens, output_tokens, cached_tokens, usd_cost, effective_date, source}`; without split rates and real usage there is nothing auditable to emit.
- The shipped cost model exposes one blended per-token rate and calculate_cost(num_tokens), which is structurally wrong: input vs output are priced asymmetrically (output 4-8x input), cached input is now a distinct ~10x-cheaper rate, non-text modalities bill per image/second/character rather than per token, some models have context-length tiers, and no rate carries an effective date or source. A finops cost ledger cannot be built on that. This research doc is the design foundation that ADR-0009 formalizes, and it gates the finops observability middleware that comes after.
- This design turns the just-landed structured pricing work (ADR-0009, 2.9.0) into an actual cost-observability capability, so a future agent picking up the finops backlog has a settled contract to implement against: what the cost event carries, where it hooks in, and what is intentionally deferred. It records the load-bearing decision that cost tracking is observe-only in v1 (never affecting program flow), which constrains every later budgeting/enforcement layer built on the event stream.
- Today `get_observability_middleware()` resolves to exactly one middleware in the observability slot — either a no-op or the logger-backed one. Finops cost tracking is a lifecycle/observability concern (ADR-0003), so it belongs in that same slot, but it must run *alongside* logging rather than displace it. Special-casing "logger + cost" would bake finops into the runtime and make every future observer another special case. The decision is to generalize the single slot into a `CompositeObservabilityMiddleware` that holds N children and fans `before_call` / `after_call` / `on_error` to each. The composite is enabled when any child is enabled, so an all-disabled profile stays a true no-op.
- Cost tracking could plausibly enforce policy — cap spend, throttle, or block a call once a budget is exceeded. The decision is to explicitly *exclude* that from the library: v1 (and the finops role generally) is observe-only. It computes cost per call and emits an event; it never affects program flow. Budgets, alerting, and enforcement are pushed to a separate downstream layer that consumes the emitted event stream, outside this library.
- Given that cost is computed from real usage and split rates (ADR-0011), a second decision governs how the result leaves the library: what the emitted unit is, where it goes, and how much the library does with it. The unit is a self-describing `AiApiCostEvent` per call that carries not only `usd_cost`, token counts, and call identity, but the *pricing provenance* — `pricing_effective_date`, `pricing_source`, `pricing_confidence`. Carrying provenance on the event is what makes it auditable: a stored event explains exactly which rate produced it, with no need to re-derive or reconcile against a moving pricing table later.
- The finops design doc was revised on 2026-07-07 and now reverses its earlier plan for a `CompositeObservabilityMiddleware` that chained a separate cost middleware (the decision captured in ADR-0012). The revised framing is that cost is not a new subsystem but a *derived field* on the observability event the logger-backed middleware already emits in `after_call`: the same model, provider, and token counts, plus a computed `usd_cost`. Because observe-only v1 introduces no new data, hook, or lifecycle, a composite that fans lifecycle calls to N children is scaffolding for a separation that does not yet exist. Building it now would add a runtime abstraction ahead of any second observer that needs it.

## Active blockers

- The shipped cost model exposes one blended per-token rate and calculate_cost(num_tokens), which is structurally wrong: input vs output are priced asymmetrically (output 4-8x input), cached input is now a distinct ~10x-cheaper rate, non-text modalities bill per image/second/character rather than per token, some models have context-length tiers, and no rate carries an effective date or source. A finops cost ledger cannot be built on that. This research doc is the design foundation that ADR-0009 formalizes, and it gates the finops observability middleware that comes after.
- The composite reuses the fail-open guarantee already implemented in `execute_observed_call`: a child that raises is logged and skipped, and provider behavior is never affected. This is the property that lets observers be additive without risk — adding cost tracking cannot break a provider call even if pricing lookup throws. The change is deliberately confined to how the slot is *built*; provider code and the call runtime are untouched, which is why it is called out as "the one architectural change."

## Decision candidates

- The current cost model exposes one blended `price_per_1k_tokens` float per model and multiplies it by a raw token count. The research shows this is structurally wrong, not merely imprecise: output is priced 4–8× input (gpt-5.5 is $5 in / $30 out), cached input is now a first-class rate roughly 10× cheaper than fresh input, non-text modalities do not price per token at all (images per image, video per second, TTS per character), and some models tier by context length (gemini-2.5-pro doubles above 200K input tokens). A single scalar cannot express any of these, so blended figures are already fiction — the OpenAI "0.0100" entries are hand-picked midpoints between input and output list prices. ([2026-07-07 11:51:24 UTC by dave-thomas](events/2026-07-07T11-51-24Z--dave-thomas--adr-inspector.md))
- Today rate data is embedded inside the provider/model classes. That conflates two things that change on completely different schedules and for different reasons: a model class describes fixed behavior and capabilities, whereas a rate is external data the provider revises on its own timeline. The research calls out the consequence — the in-code numbers are stale and partly invented, several advertised models are retired or past their shutdown date, and no rate carries an effective date or source, so no cost figure is auditable. A finops layer cannot stand on unauditable, drift-prone numbers. ([2026-07-07 11:51:25 UTC by dave-thomas](events/2026-07-07T11-51-25Z--dave-thomas--adr-inspector.md))
- Cost today is computed by `calculate_cost(num_tokens)` — one blended rate times a single token count. Once input, cached-input, and output carry asymmetric rates, blending is wrong regardless of how good the rate table is, because it discards the actual input/output mix of each call. The decision is to change the cost-computation contract: cost is derived from the real, provider-reported usage — the `provider_prompt_tokens` and `provider_completion_tokens` the observability runtime already captures — multiplied by the split rates from the pricing descriptor, yielding an auditable cost event rather than an estimate. This is why the pricing redesign must land before the finops middleware: the middleware hooks `after_call`, reads actual token counts, looks up `AIModelPricing`, and emits `{model, input_tokens, output_tokens, cached_tokens, usd_cost, effective_date, source}`; without split rates and real usage there is nothing auditable to emit. ([2026-07-07 11:51:26 UTC by dave-thomas](events/2026-07-07T11-51-26Z--dave-thomas--adr-inspector.md))
- This design turns the just-landed structured pricing work (ADR-0009, 2.9.0) into an actual cost-observability capability, so a future agent picking up the finops backlog has a settled contract to implement against: what the cost event carries, where it hooks in, and what is intentionally deferred. It records the load-bearing decision that cost tracking is observe-only in v1 (never affecting program flow), which constrains every later budgeting/enforcement layer built on the event stream. ([2026-07-07 16:41:17 UTC by 2355287-davecthomas](events/2026-07-07T16-41-17Z--2355287-davecthomas--thread_451c3cb5-1bf1-4f06-911e-bb274923e3b1--turn_d787e6452f.md))
- Today `get_observability_middleware()` resolves to exactly one middleware in the observability slot — either a no-op or the logger-backed one. Finops cost tracking is a lifecycle/observability concern (ADR-0003), so it belongs in that same slot, but it must run *alongside* logging rather than displace it. Special-casing "logger + cost" would bake finops into the runtime and make every future observer another special case. The decision is to generalize the single slot into a `CompositeObservabilityMiddleware` that holds N children and fans `before_call` / `after_call` / `on_error` to each. The composite is enabled when any child is enabled, so an all-disabled profile stays a true no-op. ([2026-07-07 16:42:32 UTC by dave-thomas](events/2026-07-07T16-42-32Z--dave-thomas--adr-inspector.md))
- Cost tracking could plausibly enforce policy — cap spend, throttle, or block a call once a budget is exceeded. The decision is to explicitly *exclude* that from the library: v1 (and the finops role generally) is observe-only. It computes cost per call and emits an event; it never affects program flow. Budgets, alerting, and enforcement are pushed to a separate downstream layer that consumes the emitted event stream, outside this library. ([2026-07-07 16:42:33 UTC by dave-thomas](events/2026-07-07T16-42-33Z--dave-thomas--adr-inspector.md))
- Given that cost is computed from real usage and split rates (ADR-0011), a second decision governs how the result leaves the library: what the emitted unit is, where it goes, and how much the library does with it. The unit is a self-describing `AiApiCostEvent` per call that carries not only `usd_cost`, token counts, and call identity, but the *pricing provenance* — `pricing_effective_date`, `pricing_source`, `pricing_confidence`. Carrying provenance on the event is what makes it auditable: a stored event explains exactly which rate produced it, with no need to re-derive or reconcile against a moving pricing table later. ([2026-07-07 16:42:34 UTC by dave-thomas](events/2026-07-07T16-42-34Z--dave-thomas--adr-inspector.md))
- The finops design doc was revised on 2026-07-07 and now reverses its earlier plan for a `CompositeObservabilityMiddleware` that chained a separate cost middleware (the decision captured in ADR-0012). The revised framing is that cost is not a new subsystem but a *derived field* on the observability event the logger-backed middleware already emits in `after_call`: the same model, provider, and token counts, plus a computed `usd_cost`. Because observe-only v1 introduces no new data, hook, or lifecycle, a composite that fans lifecycle calls to N children is scaffolding for a separation that does not yet exist. Building it now would add a runtime abstraction ahead of any second observer that needs it. ([2026-07-07 17:30:00 UTC by dave-thomas](events/2026-07-07T17-30-00Z--dave-thomas--adr-inspector.md))
- The revised finops design doc changes how the cost result *leaves* the library. ADR-0014 made a pluggable `AiApiCostSink` (`record(event) -> None`) the v1 emission mechanism, with a default log sink. The revision recognizes that in observe-only v1 the sink abstraction is premature: Python's standard logging already provides topic-based routing via logger names, handlers, and filters, so a dedicated cost log topic gives applications everything they need to filter and route cost events to a separate destination without any new abstraction. The pluggable sink only becomes justified when events must reach a **billing-grade numeric destination in-process** (a Prometheus counter, a cost database, a billing API) where pushing Decimal money through log strings and re-parsing is fragile. That is a destination choice, not a reason to add the interface up front. ([2026-07-07 17:30:01 UTC by dave-thomas](events/2026-07-07T17-30-01Z--dave-thomas--adr-inspector.md))

## Next likely steps

- Phase 1 is a behavior-preserving CompositeObservabilityMiddleware refactor; cached-token cost accuracy is deferred to Phase 3 because the result summary does not yet capture per-provider cached-token counts (OpenAI prompt_tokens_details.cached_tokens, Gemini cached_content_token_count, Anthropic cache read/write). This is a design-for-review with no code written yet — a candidate for promotion to an ADR.
- Promote to ADR superseding ADR-0014. Phase 1 emits cost events on the cost log topic with provenance and `ObservabilitySettingsModel` topic override; `AiApiCostSink` + `cost_sink` selector are Phase 2, added when a non-log destination forces it.

## Relevant event shards

- [2026-07-07 11:50:02 UTC by 2355287-davecthomas](events/2026-07-07T11-50-02Z--2355287-davecthomas--thread_451c3cb5-1bf1-4f06-911e-bb274923e3b1--turn_c8372fc1c0.md)
- [2026-07-07 11:51:24 UTC by dave-thomas](events/2026-07-07T11-51-24Z--dave-thomas--adr-inspector.md)
- [2026-07-07 11:51:25 UTC by dave-thomas](events/2026-07-07T11-51-25Z--dave-thomas--adr-inspector.md)
- [2026-07-07 11:51:26 UTC by dave-thomas](events/2026-07-07T11-51-26Z--dave-thomas--adr-inspector.md)
- [2026-07-07 13:09:39 UTC by 2355287-davecthomas](events/2026-07-07T13-09-39Z--2355287-davecthomas--thread_451c3cb5-1bf1-4f06-911e-bb274923e3b1--turn_0aeb9937ef.md)
- [2026-07-07 16:41:17 UTC by 2355287-davecthomas](events/2026-07-07T16-41-17Z--2355287-davecthomas--thread_451c3cb5-1bf1-4f06-911e-bb274923e3b1--turn_d787e6452f.md)
- [2026-07-07 16:42:32 UTC by dave-thomas](events/2026-07-07T16-42-32Z--dave-thomas--adr-inspector.md)
- [2026-07-07 16:42:33 UTC by dave-thomas](events/2026-07-07T16-42-33Z--dave-thomas--adr-inspector.md)
- [2026-07-07 16:42:34 UTC by dave-thomas](events/2026-07-07T16-42-34Z--dave-thomas--adr-inspector.md)
- [2026-07-07 17:30:00 UTC by dave-thomas](events/2026-07-07T17-30-00Z--dave-thomas--adr-inspector.md)
